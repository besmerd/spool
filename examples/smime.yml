---
defaults:
  sender: sender@example.org
  recipients: recipient@example.org

mails:
  - name: smime-signed
    description: A S/MIME signed message (signature detached)
    subject: S/MIME Signed Message
    from_key: smime/sender.key.pem
    from_crt: smime/sender.crt.pem
    text_body: |
        A sign of our times.
    attachments:
      - test.txt
      - test.png
